apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../services/ingress-nginx

namePrefix: private-shared-
namespace: kube-system

patchesStrategicMerge:
- service-patch.yaml
