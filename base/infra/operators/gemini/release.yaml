apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gemini
spec:
  releaseName: gemini
  chart:
    spec:
      chart: gemini
      sourceRef:
        kind: HelmRepository
        name: fairwinds
        namespace: flux-system
      version: "2.1.3"
  interval: 1h0m0s
  install:
    remediation:
      retries: 3