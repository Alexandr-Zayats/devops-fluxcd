<filter activity.log>
  @type record_transformer
  enable_ruby
  <record>
    # ${Time.at(record["timestamp"] || Time.now.to_i)}
    index_name ${"activity-log-" + record['env'] + "-" + record['service'] + "-" + Time.at(record["timestamp"]).strftime('%Y.%m.%d')}
  </record>
</filter>
