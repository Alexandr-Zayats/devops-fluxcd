<filter **>
  @type record_transformer
  enable_ruby
  <record>
    # prefix ${record['kubernetes']['namespace_name'] + "-" + record['kubernetes']['pod_name'].split('-')[0]}
    prefix ${record['kubernetes']['namespace_name']}
  </record>
</filter>
