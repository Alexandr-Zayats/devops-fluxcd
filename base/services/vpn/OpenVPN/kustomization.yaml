---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pv.yaml
  - release.yaml

configMapGenerator:
  - name: openvpn-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true
