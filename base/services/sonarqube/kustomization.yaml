---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sonarqube-oidc-secret.yaml
  - release.yaml

configMapGenerator:
  - name: sonarqube-values
    files:
      - values.yaml
    behavior: create
generatorOptions:
  disableNameSuffixHash: true
