---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
  # - ingress.yaml

configMapGenerator:
  - name: sentry-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true
